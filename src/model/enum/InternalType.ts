const enum InternalType {
  MERGE_REQUEST = '__MERGE_REQUEST',
  PULL_REQUEST = '__PULL_REQUEST',
  GITHUB_COMMIT = '__GITHUB_COMMIT',
  GITLAB_COMMIT = '__GITLAB_COMMIT',
}

export default InternalType;
